import Colors from '../colors';

const HeaderStyle = {
  bar: {
    backgroundColor: Colors.Primary,
  },
  title: {
    fontWeight: 'bold',
    color: Colors.White,
    textAlign: 'center',
  },
};

export default HeaderStyle;
